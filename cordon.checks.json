{
  "$schema": "https://raw.githubusercontent.com/joeseverino/cordon/main/checks/config.schema.json",
  "idempotence": {
    "command": null
  },
  "commands": [
    {
      "id": "demo-failure",
      "name": "Intentional demo failure",
      "effect": "read",
      "exec": { "cmd": "sh", "args": ["-c", "echo 'pretend a spec found a problem here'; exit 1"] },
      "fix": "This is a deliberate demo — delete the demo-failure entry from cordon.checks.json to go green."
    },
    {
      "id": "demo-skip",
      "name": "Intentional capability skip",
      "effect": "local_write",
      "requires": ["a-tool-that-is-not-installed"],
      "exec": { "cmd": "true" }
    }
  ]
}
